version: '3.8'

services:
  front-rrhh:
    build: .
    container_name: front_rrhh
    ports:
      - "3000:3000"
    environment:
      - VITE_API_URL=http://localhost:8000/api/v1
    networks:
      - asdf-network

networks:
  asdf-network:
    external: true
