version: '3.8'

services:
  front-mantencion:
    build: .
    container_name: front_mantencion
    ports:
      - "3001:3000"
    environment:
      - VITE_API_URL=http://localhost:8001/api/v1
    networks:
      - asdf-network

networks:
  asdf-network:
    external: true
